const config = {
    canvasWidth: 450,
    canvasHeight: 450,
    maxScoreKeyName: "$name_for_saving_max_score_in_local_storage$",
    defaultScore: 0,
    leftKey: 37,
    upKey: 38,
    rightKey: 39,
    downKey:40,
    nodeLen: 4,
    numNodesInHeight: 100,
    numNodesInWidht: 100,
    defaultInterval: 100,
    SCORE_STEP: 10
};

export default config;